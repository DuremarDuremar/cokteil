import React from "react";
import styled from "styled-components";

const Content = styled.div`
  width: 60px;
  height: 120px;
  background: #fffdf5;
  position: fixed;
  top: 0;
`;

const ScrollToTop = () => {
  return <Content>scrollToTop</Content>;
};

export default ScrollToTop;
